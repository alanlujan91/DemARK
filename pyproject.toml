[project]
name = "demark"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "econ-ark",
    "jupyter>=1.1.1",
    "jupytext>=1.17.2",
    "mystmd>=1.6.0",
]

[tool.uv.sources]
econ-ark = { git = "https://github.com/econ-ark/hark", rev = "master" }
